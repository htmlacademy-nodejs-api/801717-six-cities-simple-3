export enum PriceLimits {
    MIN_PRICE = 100,
    MAX_PRICE = 100000,
}
export enum RatingLimits {
  MIN_RATING = 1,
  MAX_RATING = 5,
}
export enum RoomsLimits {
  MIN_ROOMS = 1,
  MAX_ROOMS = 8,
}
export enum GuestsLimits {
  MIN_GUESTS = 1,
  MAX_GUESTS = 10,
}
export enum CommentsLimits {
  MIN_COMMENTS = 0,
  MAX_COMMENTS = 100,
}
export enum WeekDayLimits {
  FIRST_WEEK_DAY = 1,
  LAST_WEEK_DAY = 7
}
